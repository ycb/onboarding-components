<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Onboarding Components - Live Demo</title>
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
            background-color: #f9fafb;
            margin: 0;
            padding: 0;
        }
        .demo-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }
        .header {
            text-align: center;
            margin-bottom: 3rem;
            background-color: white;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        }
        .header h1 {
            font-size: 2.5rem;
            font-weight: bold;
            color: #1f2937;
            margin-bottom: 0.5rem;
        }
        .header p {
            font-size: 1.125rem;
            color: #4b5563;
            margin-bottom: 1.5rem;
        }
        .header .buttons {
            display: flex;
            justify-content: center;
            gap: 1rem;
            flex-wrap: wrap;
        }
        .header .button {
            display: inline-flex;
            align-items: center;
            padding: 0.75rem 1.5rem;
            border-radius: 0.375rem;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.2s ease-in-out;
        }
        .header .button-primary {
            background-color: #2563eb;
            color: white;
        }
        .header .button-primary:hover {
            background-color: #1d4ed8;
        }
        .header .button-secondary {
            background-color: white;
            color: #2563eb;
            border: 1px solid #2563eb;
        }
        .header .button-secondary:hover {
            background-color: #eff6ff;
        }
        .demo-section {
            background-color: white;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            margin-bottom: 2rem;
        }
        .demo-section h2 {
            font-size: 1.75rem;
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 1rem;
        }
        .demo-section p {
            color: #4b5563;
            margin-bottom: 1.5rem;
        }
        .code-snippet {
            background-color: #f3f4f6;
            border-radius: 0.375rem;
            padding: 1rem;
            overflow-x: auto;
            margin-top: 1.5rem;
            font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
            font-size: 0.875rem;
        }
        .code-snippet pre {
            margin: 0;
            color: #374151;
        }
        .footer-section {
            text-align: center;
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid #e5e7eb;
            color: #6b7280;
            font-size: 0.875rem;
        }
        .footer-section a {
            color: #2563eb;
            text-decoration: none;
        }
        .footer-section a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="demo-container">
        <!-- Header Section -->
        <div class="header">
            <h1>Onboarding Components</h1>
            <p>A collection of reusable React components for user onboarding flows</p>
            <div class="buttons">
                <a href="https://github.com/ycb/onboarding-components" target="_blank" class="button button-primary">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-right: 8px;">
                        <path d="M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.44-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.27 1.15-.27 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"></path>
                        <path d="M9 18c-4.51 2-5-2-7-2"></path>
                    </svg>
                    View on GitHub
                </a>
                <button onclick="copyInstallCommand()" class="button button-secondary">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-right: 8px;">
                        <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
                    </svg>
                    Copy Install Command
                </button>
            </div>
        </div>

        <!-- Live Demo Section -->
        <div class="demo-section">
            <h2>Live Demo</h2>
            <p>Try out the interactive components below. You can upload files, enter text, and see the components in action.</p>
            <div id="demo-root"></div>
        </div>

        <!-- Usage Example Section -->
        <div class="demo-section">
            <h2>Usage Example</h2>
            <p>Here's how to use the ContentUploadModule component in your React application:</p>
            <div class="code-snippet">
                <pre><code>import React from 'react';
import { ContentUploadModule } from '@ycb/onboarding-components';

function App() {
  const handleFileUpload = (file) => {
    console.log('File uploaded:', file);
  };

  const handleTextInput = (text) => {
    console.log('Text input:', text);
  };

  const handleUploadError = (error) => {
    console.error('Upload error:', error);
  };

  return (
    &lt;ContentUploadModule
      onFileUpload={handleFileUpload}
      onTextInput={handleTextInput}
      onUploadError={handleUploadError}
    /&gt;
  );
}

export default App;</code></pre>
            </div>
        </div>

        <!-- API Documentation Section -->
        <div class="demo-section">
            <h2>API Documentation</h2>
            <p>Component props and configuration options:</p>
            <div class="code-snippet">
                <pre><code>interface ContentUploadModuleProps {
  onFileUpload?: (file: File) => void;
  onTextInput?: (text: string) => void;
  onUploadError?: (error: string) => void;
}

interface FileUploadCardProps {
  type: 'resume' | 'linkedin' | 'coverLetter';
  title: string;
  description: string;
  required?: boolean;
  onFileUpload?: (file: File) => void;
  onTextInput?: (text: string) => void;
  onUploadError?: (error: string) => void;
}</code></pre>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer-section">
            <p>Made with ❤️ for better user onboarding experiences</p>
            <p>
                <a href="https://github.com/ycb/onboarding-components/blob/main/LICENSE" target="_blank">MIT License</a> | 
                <a href="https://github.com/ycb/onboarding-components/issues" target="_blank">Report Issues</a> | 
                <a href="https://github.com/ycb/onboarding-components/discussions" target="_blank">Discussions</a>
            </p>
        </div>
    </div>

    <!-- Load the built components -->
    <script src="./dist/index.umd.js"></script>
    
    <script type="text/babel">
        const { useState } = React;
        const { ContentUploadModule } = OnboardingComponents;

        function DemoApp() {
            const [uploadedFiles, setUploadedFiles] = useState([]);
            const [textInputs, setTextInputs] = useState({});

            const handleFileUpload = (file) => {
                console.log('File uploaded:', file);
                setUploadedFiles(prev => [...prev, {
                    name: file.name,
                    size: file.size,
                    type: file.type,
                    timestamp: new Date().toISOString()
                }]);
                alert(`File uploaded: ${file.name} (${(file.size / 1024).toFixed(1)} KB)`);
            };

            const handleTextInput = (text) => {
                console.log('Text input:', text);
                setTextInputs(prev => ({
                    ...prev,
                    [Date.now()]: text.substring(0, 100) + (text.length > 100 ? '...' : '')
                }));
                alert(`Text input received: ${text.substring(0, 50)}${text.length > 50 ? '...' : ''}`);
            };

            const handleUploadError = (error) => {
                console.error('Upload error:', error);
                alert(`Upload error: ${error}`);
            };

            return React.createElement('div', {
                className: 'space-y-6'
            }, [
                React.createElement(ContentUploadModule, {
                    key: 'content-upload',
                    onFileUpload: handleFileUpload,
                    onTextInput: handleTextInput,
                    onUploadError: handleUploadError
                }),
                
                // Show uploaded files and text inputs
                (uploadedFiles.length > 0 || Object.keys(textInputs).length > 0) && React.createElement('div', {
                    key: 'results',
                    className: 'mt-8 p-4 bg-gray-50 rounded-lg'
                }, [
                    React.createElement('h3', {
                        key: 'title',
                        className: 'text-lg font-semibold mb-4'
                    }, 'Upload Results'),
                    
                    uploadedFiles.length > 0 && React.createElement('div', {
                        key: 'files',
                        className: 'mb-4'
                    }, [
                        React.createElement('h4', {
                            key: 'files-title',
                            className: 'font-medium mb-2'
                        }, 'Uploaded Files:'),
                        ...uploadedFiles.map((file, index) => 
                            React.createElement('div', {
                                key: `file-${index}`,
                                className: 'text-sm text-gray-600 mb-1'
                            }, `${file.name} (${(file.size / 1024).toFixed(1)} KB)`)
                        )
                    ]),
                    
                    Object.keys(textInputs).length > 0 && React.createElement('div', {
                        key: 'texts',
                        className: 'mb-4'
                    }, [
                        React.createElement('h4', {
                            key: 'texts-title',
                            className: 'font-medium mb-2'
                        }, 'Text Inputs:'),
                        ...Object.entries(textInputs).map(([key, text]) => 
                            React.createElement('div', {
                                key: `text-${key}`,
                                className: 'text-sm text-gray-600 mb-1'
                            }, text)
                        )
                    ])
                ])
            ]);
        }

        // Copy install command function
        function copyInstallCommand() {
            const command = 'npm install @ycb/onboarding-components';
            navigator.clipboard.writeText(command).then(() => {
                alert('Install command copied to clipboard!');
            }).catch(() => {
                // Fallback for older browsers
                const textArea = document.createElement('textarea');
                textArea.value = command;
                document.body.appendChild(textArea);
                textArea.select();
                document.execCommand('copy');
                document.body.removeChild(textArea);
                alert('Install command copied to clipboard!');
            });
        }

        // Render the demo
        ReactDOM.render(React.createElement(DemoApp), document.getElementById('demo-root'));
    </script>
</body>
</html>